# Phase 8.5 (단계 ②b) 라이브러리 그리드 썸네일 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 라이브러리 그리드 셀의 종류별 SF 아이콘을 QuickLook 썸네일로 교체한다(전 파일 시도 + 실패 시 아이콘 폴백, 그리드 전용, 화면 밖 생성 취소).

**Architecture:** `ThumbnailService`(@MainActor, QLThumbnailGenerator + NSCache)가 url별 썸네일을 비동기 생성·캐시한다. `LibraryGridCell`이 `.task(id: url)`로 파일일 때만 요청하고, 있으면 `Image(nsImage:)`·없으면 기존 SF 아이콘을 보인다. LazyVGrid가 보이는 셀만 만들고 `.task`가 사라질 때 취소돼 1000+ 폴더도 안전.

**Tech Stack:** Swift 5.9 / SwiftUI, QuickLookThumbnailing(macOS 내장), AppKit NSImage/NSCache, XCTest.

## Global Constraints

- macOS 14+, Swift 5.9+, SPM. 비샌드박스(QuickLook 파일 접근 OK).
- 코드 주석·커밋 메시지 **한국어**. '박다/박는다/박았다' 금지. 신규 기능은 별도 파일.
- **읽기 전용** — 썸네일 생성만, 파일 이동·삭제·변경 없음.
- 대상: 전 파일 QLThumbnailGenerator 시도 + **실패/미지원이면 SF 아이콘 폴백**. 폴더는 폴더 아이콘(썸네일 없음).
- **그리드만**(LibraryGridCell). 리스트(LibraryListCell)는 손대지 않음.
- 캐시 키에 **mtime 포함**(편집 시 갱신). NSCache(메모리 압박 시 자동 비움).
- 커밋 푸터 2줄:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01Qeysoi8eVrcjX87xBeZ4Pf`
- 모듈명 `CmdMD`. 테스트 `Tests/CmdMDTests/`. `swift test`엔 정식 Xcode 필요. ⚠️ 기존 `AppParaRoutingTests` 환경 실패는 무관.

---

### Task 1: ThumbnailService (QLThumbnailGenerator + NSCache)

**Files:**
- Create: `Sources/Services/ThumbnailService.swift`
- Test: `Tests/CmdMDTests/ThumbnailServiceTests.swift`

**Interfaces:**
- Produces:
  - `@MainActor final class ThumbnailService` — `static let shared`, `func thumbnail(for url: URL, pointSize: CGFloat, scale: CGFloat) async -> NSImage?`
  - `static func cacheKey(url: URL, pointSize: CGFloat, scale: CGFloat, mtime: TimeInterval) -> String`

- [ ] **Step 1: 실패하는 테스트 작성** (순수 cacheKey만 — QL 생성·NSCache는 환경의존, 빌드+수동)

```swift
import XCTest
@testable import CmdMD

final class ThumbnailServiceTests: XCTestCase {
    private let u = URL(fileURLWithPath: "/a/b/photo.png")

    func testCacheKeyStableForSameInputs() {
        let k1 = ThumbnailService.cacheKey(url: u, pointSize: 64, scale: 2, mtime: 100)
        let k2 = ThumbnailService.cacheKey(url: u, pointSize: 64, scale: 2, mtime: 100)
        XCTAssertEqual(k1, k2)
    }

    func testCacheKeyChangesWithMtime() {
        let k1 = ThumbnailService.cacheKey(url: u, pointSize: 64, scale: 2, mtime: 100)
        let k2 = ThumbnailService.cacheKey(url: u, pointSize: 64, scale: 2, mtime: 200)
        XCTAssertNotEqual(k1, k2)
    }

    func testCacheKeyChangesWithSizeScaleAndURL() {
        let base = ThumbnailService.cacheKey(url: u, pointSize: 64, scale: 2, mtime: 100)
        XCTAssertNotEqual(base, ThumbnailService.cacheKey(url: u, pointSize: 128, scale: 2, mtime: 100))
        XCTAssertNotEqual(base, ThumbnailService.cacheKey(url: u, pointSize: 64, scale: 1, mtime: 100))
        XCTAssertNotEqual(base, ThumbnailService.cacheKey(url: URL(fileURLWithPath: "/a/b/other.png"), pointSize: 64, scale: 2, mtime: 100))
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: `swift test --filter ThumbnailServiceTests`
Expected: 컴파일 실패("cannot find 'ThumbnailService'").

- [ ] **Step 3: 구현**

```swift
import AppKit
import QuickLookThumbnailing

/// 라이브러리 그리드용 썸네일 생성·캐시. QLThumbnailGenerator로 비동기 생성하고
/// NSCache로 재사용한다. 못 만드는 타입은 nil(호출부가 SF 아이콘 폴백). 읽기 전용.
@MainActor
final class ThumbnailService {
    static let shared = ThumbnailService()

    private let cache = NSCache<NSString, NSImage>()

    private init() {}

    /// url의 썸네일(pointSize·scale 기준). 캐시 히트면 즉시, 미스면 생성·캐시.
    /// 실패/미지원/접근불가면 nil.
    func thumbnail(for url: URL, pointSize: CGFloat, scale: CGFloat) async -> NSImage? {
        let mtime = (try? url.resourceValues(forKeys: [.contentModificationDateKey]))?
            .contentModificationDate?.timeIntervalSince1970 ?? 0
        let key = Self.cacheKey(url: url, pointSize: pointSize, scale: scale, mtime: mtime) as NSString

        if let cached = cache.object(forKey: key) { return cached }

        let request = QLThumbnailGenerator.Request(
            fileAt: url,
            size: CGSize(width: pointSize, height: pointSize),
            scale: scale,
            representationTypes: .thumbnail
        )

        guard let rep = try? await QLThumbnailGenerator.shared.generateBestRepresentation(for: request) else {
            return nil
        }
        let image = NSImage(cgImage: rep.cgImage, size: NSSize(width: pointSize, height: pointSize))
        cache.setObject(image, forKey: key)
        return image
    }

    /// 캐시 키(순수). url·크기·scale·mtime 조합 — mtime이 바뀌면(편집) 새 키.
    static func cacheKey(url: URL, pointSize: CGFloat, scale: CGFloat, mtime: TimeInterval) -> String {
        "\(url.path)|\(pointSize)|\(scale)|\(mtime)"
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: `swift test --filter ThumbnailServiceTests`
Expected: PASS (3 tests).

- [ ] **Step 5: 빌드 확인**

Run: `swift build`
Expected: 성공(QuickLookThumbnailing 링크 OK).

- [ ] **Step 6: 커밋**

```bash
git add Sources/Services/ThumbnailService.swift Tests/CmdMDTests/ThumbnailServiceTests.swift
git commit -m "기능(라이브러리): ThumbnailService(QLThumbnailGenerator+NSCache) 추가"
```

---

### Task 2: LibraryGridCell에 썸네일 표시

**Files:**
- Modify: `Sources/Views/LibraryView.swift` (`LibraryGridCell` ~169-205)
- Test: 없음(SwiftUI UI) — `swift build` + 수동. 생성·캐시 로직은 Task 1에서 단위테스트.

**Interfaces:**
- Consumes: `ThumbnailService.shared.thumbnail(for:pointSize:scale:)`(Task 1); 기존 `FileTreeItem`(`url`/`name`/`icon`/`isDirectory`), `ParaCategory`/`ParaLens`, `cellIcon`/`iconColor`.

- [ ] **Step 1: LibraryGridCell에 썸네일 상태 + 비동기 로드 + 표시 추가**

`Sources/Views/LibraryView.swift`의 `LibraryGridCell`을 아래로 교체한다. 이미지 영역만 썸네일/아이콘 분기로 바꾸고, 이름·padding·archive opacity·탭 컨테이너는 그대로. 폴더는 썸네일 안 만들고 폴더 아이콘 유지:

```swift
struct LibraryGridCell: View {
    @Environment(AppState.self) private var appState
    let item: FileTreeItem

    @State private var thumbnail: NSImage?

    private var paraCategory: ParaCategory {
        ParaLens.classify(item.url, under: appState.currentFolder)
    }

    var body: some View {
        VStack(spacing: 6) {
            imageArea
                .frame(width: 64, height: 64)

            Text(item.name)
                .font(.caption)
                .lineLimit(2)
                .multilineTextAlignment(.center)
                .foregroundStyle(.primary)
        }
        .padding(8)
        .frame(maxWidth: .infinity)
        .opacity(paraCategory == .archive ? 0.45 : 1.0)
        .contentShape(Rectangle())
        .task(id: item.url) {
            // 파일만 썸네일 생성(폴더는 폴더 아이콘 유지). 셀이 사라지면 .task가 취소된다.
            guard !item.isDirectory else { return }
            thumbnail = await ThumbnailService.shared.thumbnail(for: item.url, pointSize: 64, scale: 2)
        }
    }

    @ViewBuilder
    private var imageArea: some View {
        if !item.isDirectory, let thumbnail {
            Image(nsImage: thumbnail)
                .resizable()
                .scaledToFit()
                .cornerRadius(4)
        } else {
            Image(systemName: cellIcon)
                .font(.system(size: 32))
                .foregroundStyle(iconColor)
        }
    }

    private var cellIcon: String {
        item.isDirectory ? "folder" : item.icon
    }

    private var iconColor: Color {
        if item.isDirectory && paraCategory == .projects {
            return .cmdsAccent
        }
        return .secondary
    }
}
```

> 주의: 기존 `LibraryGridCell`의 `onTapGesture { handleTap(item:) }`는 `gridView`의 ForEach에서 셀 바깥에 붙어 있으므로(LibraryView.swift:114-117) 셀 본문 교체와 무관하게 보존된다. `LibraryListCell`은 건드리지 않는다.

- [ ] **Step 2: 빌드 + 회귀**

Run: `swift build`  → 성공.
Run: `swift test`  → Task 1 신규 포함 통과, AppParaRoutingTests 외 신규 실패 없음.

- [ ] **Step 3: 커밋**

```bash
git add Sources/Views/LibraryView.swift
git commit -m "기능(라이브러리): 그리드 셀에 QuickLook 썸네일 표시(아이콘 폴백)"
```

- [ ] **Step 4: 수동 검증**

`bash scripts/package_app.sh` 후 `.app` 실행 → 라이브러리 그리드에서:
- 이미지·PDF·영상 파일에 실제 썸네일 표시.
- HWP·md·txt 등 QL 미지원/문서는 기존 SF 아이콘 폴백.
- 폴더는 폴더 아이콘(projects=accent·archive dim 유지).
- 큰 폴더에서 스크롤·드릴인 시 멈춤 없음. 같은 폴더 재방문 시 캐시로 즉시.

---

## Self-Review

**Spec coverage:**
- §3 ThumbnailService(QL+NSCache·thumbnail·cacheKey 순수) → Task 1 ✓
- §3 LibraryGridCell 썸네일 상태·.task·표시·폴더 제외·archive/이름/탭 보존 → Task 2 ✓
- §4 데이터흐름(보이는 셀만·.task 취소) → Task 2 .task(id:) + LazyVGrid(기존) ✓
- §5 에러(QL 실패→nil→아이콘 폴백) → Task 1 thumbnail nil + Task 2 imageArea 분기 ✓
- §6 테스트(cacheKey 순수) → Task 1 Step1 ✓ / §7 게이트 → Task 1 Step4·5·Task 2 Step2·4 ✓
- §2 그리드만·읽기전용·mtime 키 → 두 태스크 반영 ✓

**Placeholder scan:** 모든 코드 step에 실제 코드. TBD 없음. ✓

**Type consistency:** `ThumbnailService.shared.thumbnail(for:pointSize:scale:)`·`cacheKey(url:pointSize:scale:mtime:)`(Task1 정의) ↔ Task2 호출·테스트 일치. `cellIcon`/`iconColor`/`paraCategory`(기존) 재사용. `NSImage`(AppKit). ✓
